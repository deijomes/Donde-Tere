import { EmailsplitPipe } from './emailsplit.pipe';

describe('EmailsplitPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailsplitPipe();
    expect(pipe).toBeTruthy();
  });
});
