name: Generate a build and push to another branch

on:
  push:
    branches:
      - Desarrollo  # Cambia esto por la rama que deseas usar

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build and Push
    permissions:
      contents: write  # Otorga permisos de escritura al GITHUB_TOKEN
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install all dependencies
        run: npm install

      - name: Build
        run: npm run build  # Genera los archivos en dist/inventario

      - name: Create or reset build branch
        run: |
          git checkout --orphan build  # Crea una rama huérfana (sin historial)
          git rm -rf .  # Elimina todos los archivos existentes
          git commit --allow-empty -m "Initial commit"  # Crea un commit vacío

      - name: Move files to root
        run: |
          mv dist/inventario/* .  # Mueve los archivos generados a la raíz
          rm -rf dist  # Elimina la carpeta dist

      - name: Push build to branch
        uses: s0/git-publish-subdir-action@develop
        env:
          REPO: self
          BRANCH: build  # Cambia esto por la rama de destino
          FOLDER: .  # Usa la raíz de la rama
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MESSAGE: "Build generado el {date} desde {sha}"